import React from "react";
import styled from 'styled-components';

const StyledTryLightMode = styled.div `
    
`

function TryLightMode():JSX.Element {
    return (
        <StyledTryLightMode>
            
        </StyledTryLightMode>
    )
}

export default TryLightMode;