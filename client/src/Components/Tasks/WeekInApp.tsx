import React from "react";
import styled from 'styled-components';

const StyledWeekInApp = styled.div `
    
`

function WeekInApp():JSX.Element {
    return (
        <StyledWeekInApp>
            
        </StyledWeekInApp>
    )
}

export default WeekInApp;